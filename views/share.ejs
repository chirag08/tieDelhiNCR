<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <title>Tie APP</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.1/css/materialize.min.css">
  <link rel="stylesheet" href="css/style.css">
  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
  <head>
    <body>
      <nav>
        <div class="nav-wrapper black">
          <ul id="nav-mobile" class="right">
            <li><a href="/">Home</a></li>
            <li><a href="discover">Discover new Places</a></li>
          </ul>

        </div>
      </nav>
      <div class="container back">
        <h1 class="center-align thin">Share your experience with fellow travelors</h1>
        <div class="row">
          <form class="col s12" method="POST" action="javascript:Alert('Extension Part!Men on work')">
            <div class="row">
              <div class="input-field col s12">
                <input id="title" name="title" type="text" class="validate">
                <label for="title">Title of Story</label>
              </div>
            </div>
            <div class="row">
              <div class="input-field col s12">
                <textarea id="exp" name="experience" class="materialize-textarea"></textarea>
                <label for="exp">Share Your Experience</label>
              </div>
            </div>
            <div class="row">
              <div class="input-field col s8">
                <input id="place" type="text" name="visitedPlace" class="validate">
                <label for="place">Visited Place</label>
              </div>
              <div class="input-field col s2">
                <input id="days" type="text" name="daysSpend" class="validate">
                <label for="days">No. of Days spend</label>
              </div>
              <div class="input-field col s2">
                <input id="total-expense" name="totalExpense" type="text" class="validate">
                <label for="total-expense">Total Expense</label>
              </div>
            </div>
            <div class="row">
              <div class="col s12 center-align">
                <button class="btn waves-effect waves-light btn-large" type="submit" name="action">Submit
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>
      <!-- jQuery (necessary for Bootstraps JavaScript plugins) -->
      <!-- Include all compiled plugins (below), or include individual files as needed -->
      <script type="text/javascript" src="js/script.js"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.1/js/materialize.min.js"></script>
    </body>
    </html>
